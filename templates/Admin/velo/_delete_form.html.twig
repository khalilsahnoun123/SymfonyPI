<form method="post" action="{{ path('app_velo_delete', {'id_velo': velo.id_velo}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ velo.id_velo) }}">
    <button class="btn">Delete</button>
</form>
